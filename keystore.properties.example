# Copy to keystore.properties and fill in your values (never commit keystore.properties).
storeFile=openwhisper-upload.jks
storePassword=YOUR_STORE_PASSWORD
keyAlias=openwhisper
keyPassword=YOUR_KEY_PASSWORD
